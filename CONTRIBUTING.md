# Contributing to stellar-x402-mcp

## Guidelines
- Follow Conventional Commits (`type(scope): description`).
- All tools must include strict Zod argument validation and error boundaries.
- Run `pnpm test` and `pnpm typecheck` before opening PRs.
